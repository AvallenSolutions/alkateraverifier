export const metadata = { title: "Verify — alkatera LCA Verifier" };

// Placeholder — the upload + standards selection flow lands in TASK-020.
export default function VerifyPage() {
  return (
    <div>
      <p className="font-mono text-label uppercase text-on-surface-subtle">
        Verify
      </p>
      <h1 className="mt-2 font-display text-h1 text-ink">Verify an LCA</h1>
      <div className="mt-6 rounded-md border border-border bg-surface p-5">
        <p className="text-body text-on-surface-muted">
          Uploading is not ready yet. It arrives in the next phase of the
          build.
        </p>
      </div>
    </div>
  );
}
